swagger: "2.0"
info:
  title: Spring Boot Example API
  description: Spring Boot API for testing Swagger
  version: 1.0.0

host: localhost
basePath: /
schemes:
  - http

paths:
  /greeting:
    get:
      summary: Returns a greeting object
      description: Yes, returns object
      produces:
        - application/json
      responses:
        200:
          description: OK